{
   "scripts": {
    "heroku-prebuild": "cd frontend && npm i @angular/cli @angular-devkit/build-angular @angular/compiler-cli typescript --save-prod && ng build --configuration production",
    "start": "cd backend && npm start"
  }
}
