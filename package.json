{
   "scripts": {
    "start": "cd frontend && npm i @angular/cli @angular-devkit/build-angular @angular/compiler-cli typescript --save-prod && ng build --configuration production ",
    "heroku-postbuild": "cd backend && npm start"
  }
}
